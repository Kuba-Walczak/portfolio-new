export default function SectionHeader({ title }: { title: string }) {
  return (
    <div className="w-full py-6 my-12">
      {/* <div className="flex items-center justify-center gap-8 text-neutral-600 font-bold tracking-wider text-5xl uppercase text-nowrap">
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
        <span>{title}</span>
      </div> */}
    </div>
  )
}
